# --- Analytics (client) ---
# Leave BLANK to disable analytics entirely — the site runs identically without it.
# PostHog project API key (EU cloud).
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com

# --- Analyst agent (GitHub Actions secrets, NOT committed) ---
# Set these as repository secrets, not in .env:
#   ANTHROPIC_API_KEY   - for the scheduled Claude review
#   POSTHOG_MCP_TOKEN   - PostHog personal API key for the MCP server (read access)
